
<% @unique_categories.each do |category| %>
<h2><%= link_to category, filter_products_path(category, [{size: 'XXL',brand: 'Zara Kids'}])%></h2>
<% end %>






<% @products.each do |product| %>
<%= link_to product.brand, product_path(product) %>
<% end %>
